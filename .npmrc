# AI Coding
# .npmrc — rang buoc cai dat phu thuoc cua workspace.
#
# `engine-strict` bat vi ADR-001 CHOT #1 pin Node LTS: mot ban Node khac major
# co the doi hanh vi `Intl.Segmenter` (ADR-001 CHOT #8) ma khong bao loi.
# Phien ban chinh xac cua tung goi do `pnpm-lock.yaml` giu, khong do file nay.

engine-strict=true
