// AI Coding
/**
 * @file generation.module.ts
 * @description Module M5 (Generation Pipeline), so huu schema `generation`.
 *
 * ⚠️ Pipeline sinh anh o quy mo san xuat thuoc MVP3. Cai thuoc MVP1 la eval kit
 * (`M1-6`) va cac cot bat buoc tren moi `generation`: `cost_usd`, `model_id`,
 * `model_version`, `attempt_no` — bon cot KHONG backfill duoc (MVP-Scope §3 F2).
 */

import { Module } from '@nestjs/common';

@Module({})
export class GenerationModule {}
