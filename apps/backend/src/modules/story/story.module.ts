// AI Coding
/**
 * @file story.module.ts
 * @description Module M1 (Ingest & Compliance Gate) va M2 (Story Intelligence),
 * so huu schema `story`.
 *
 * Pham vi cua MVP1 theo Roadmap §3.2: ingest hai pha + kiem opt-out Dieu 37b,
 * text clean deterministic la buoc DAU TIEN, Story Bible extraction, timeline
 * state resolver. Hien tai module con rong — tung phan duoc them theo Story
 * tuong ung trong `docs/022-User-Stories/Backlog/`.
 */

import { Module } from '@nestjs/common';

@Module({})
export class StoryModule {}
